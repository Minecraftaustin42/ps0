(async()=>{const panel=document.getElementById('mainPanel');const assets=await api('/api/assets');panel.innerHTML=`<h2>Creator Marketplace</h2><div class='grid'>${assets.map(a=>`<div class='card'><b>${a.name}</b><p>Creator: ${a.creator}</p><p>Type: ${a.type}</p><p>Price: ${a.price} Sculpt Coins</p><button class='btn' onclick='buy("${a.id}")'>Buy</button></div>`).join('')||'No assets yet.'}</div>`;window.buy=async(id)=>{try{await api('/api/marketplace/buy',{method:'POST',body:JSON.stringify({id})});alert('Bought (placeholder)');}catch(e){alert(e.message)}}})();
