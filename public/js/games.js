(async()=>{const panel=document.getElementById('mainPanel');const games=await api('/api/games');panel.innerHTML=`<h2>Community Games</h2><div class='grid'>${games.filter(g=>g.published).map(g=>`<div class='card'><h3>${g.name}</h3><p>By ${g.creator}</p><p>${g.description}</p><p>Visits ${g.visits||0} | Likes ${g.likes||0}</p><a class='btn' href='/game/${g.id}'>Details</a></div>`).join('')||'No published games yet.'}</div>`;})();
